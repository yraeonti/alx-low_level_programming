#include<stdio.h>
#include<string.h>
#include "main.h"

/**
 * _strcpy - entry point
 * @dest: pointer to char
 * @src: pointer to char
 * Return: pointer to char
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
