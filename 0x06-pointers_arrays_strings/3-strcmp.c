#include<stdio.h>
#include<string.h>
#include "main.h"

/**
 * _strcmp - entry point
 * @s1: pointer to char
 * @s2: pointer to char
 * Return: char
 */
char _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
