#include<stdio.h>
#include<string.h>
#include "main.h"

/**
 * _strcat - entry point
 * @dest: pointer to char
 * @src: pointer to char
 * Return: void
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
